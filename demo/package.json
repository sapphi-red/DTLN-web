{
  "name": "dtln-web-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:all": "cd .. && npm run build && cd demo && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@typescript/lib-dom": "npm:@types/web@^0.0.47",
    "vite": "^2.6.14",
    "vite-plugin-static-copy": "^0.1.0"
  },
  "dependencies": {
    "@sapphi-red/dtln-web": "file:.."
  }
}
